//--------------------------------------------------------------------------------------
// screen
//--------------------------------------------------------------------------------------
pub const fps: f32 = 60;
pub const screenWidth: i16 = 400;
pub const screenHeight: i16 = 800;
pub const screenCenterX: i16 = @divTrunc(screenWidth, 2);
pub const screenCenterY: i16 = @divTrunc(screenHeight, 2);
pub const screenCellSize: i16 = @divTrunc(screenWidth, 5);
